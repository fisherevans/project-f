# This file is kept for reference but not used.
# The project uses a custom GitHub Actions workflow (.github/workflows/release.yml)
# because GoReleaser doesn't handle CGO cross-compilation well for OpenGL/GLFW dependencies.
#
# The workflow builds on native platforms (windows-latest, macos-latest) to ensure
# proper CGO support and includes game assets in the release packages.