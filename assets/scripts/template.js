// AUTO-GENERATED TEMPLATE - Copy and customize for your entity scripts
// Generated at 2025-10-23T00:10:25-04:00 by go generate
// @ts-check

/** @type {import('./global').EntityHandler} */
const handler = {
    Init(self, world, state) {
        return {
            state: state || {},
        };
    },

    OnInteract(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnDialogueComplete(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnChatterComplete(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnTimerComplete(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnEntityZoneActivity(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnWorldStateUpdated(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    },

    OnCombatComplete(self, world, state, event) {
        return {
            state: state,
            effects: []
        };
    }
};
